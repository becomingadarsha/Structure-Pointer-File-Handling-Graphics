#include<stdio.h>
int main()
{
	int n,i,a[100];
	int *p;
	printf("How many numbers?");
	scanf("%d",&n);
	p=&a[0];
	printf("Enter numebrs:\n");
	for(i=0;i<n;i++)
	{
		scanf("%d",&*(p+i));
	}
	printf("Even numbers:\n");
	for(i=0;i<n;i++)
	{
		if((*p+i)%2==0)
		{
			printf("%d\n",*(p+i));
		}
	}
	
}
